@{
    Layout = "_Layout";
    ViewData["Title"] = "Detalle de Usuario";
    var userId = ViewBag.UserId;
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="fw-bold mb-0">
        <i class="bi bi-person-lines-fill text-primary me-2"></i>Detalle de Usuario
    </h4>
    <a href="/Admin/Users" class="btn btn-outline-secondary rounded-3">
        <i class="bi bi-arrow-left me-2"></i>Usuarios
    </a>
</div>

<div id="alertContainer"></div>

<div class="card border-0 shadow-sm rounded-4">
    <div class="card-body p-5 text-center">
        <i class="bi bi-person-lines-fill text-primary" style="font-size: 4rem;"></i>
        <h5 class="fw-bold mt-3 mb-2">Detalle de Usuario</h5>
        <p class="text-muted">Esta sección está en construcción.</p>
        <a href="/Admin/Users" class="btn btn-primary rounded-3 mt-2">
            <i class="bi bi-arrow-left me-2"></i>Volver a Usuarios
        </a>
    </div>
</div>
