<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Sistema Nativa</title>
    <link href="~/css/DemoMint.css.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="d-flex flex-column min-vh-100">

<<<<<<< HEAD
@using System.Security.Claims
@{
    var userName    = User.FindFirstValue(ClaimTypes.Name) ?? "Usuario";
    var userRole    = User.FindFirstValue(ClaimTypes.Role) ?? "";
    var currentPath = ViewContext.HttpContext.Request.Path.Value ?? "";
}

<!-- NAVBAR AUTENTICADO -->
<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container-fluid px-4">
        <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/Landing/Project">
            <i class="bi bi-tree-fill fs-4"></i><span>Sistema Nativa</span>
        </a>
        <button class="navbar-toggler border-0" type="button"
                data-bs-toggle="collapse" data-bs-target="#navMain">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navMain">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link @(currentPath == "/Home" || currentPath == "/" ? "active" : "")" href="/Home">
                        <i class="bi bi-speedometer2 me-1"></i>Dashboard
                    </a>
                </li>
                @if (userRole == "Dueno")
                {
                    <li class="nav-item">
                        <a class="nav-link @(currentPath.StartsWith("/Dueno/Fincas") ? "active" : "")" href="/Dueno/Fincas"><i class="bi bi-map me-1"></i>Mis Fincas</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentPath.StartsWith("/Dueno/Pagos") ? "active" : "")" href="/Dueno/Pagos"><i class="bi bi-cash-coin me-1"></i>Mis Pagos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentPath.StartsWith("/Dueno/CuentaBancaria") ? "active" : "")" href="/Dueno/CuentaBancaria"><i class="bi bi-bank me-1"></i>Mi IBAN</a>
                    </li>
                }
                @if (userRole == "Ingeniero")
                {
                    <li class="nav-item">
                        <a class="nav-link @(currentPath.StartsWith("/Ingeniero/Cola") || currentPath.StartsWith("/Ingeniero/Fincas") ? "active" : "")" href="/Ingeniero/Cola"><i class="bi bi-list-task me-1"></i>Cola de Revisión</a>
                    </li>
                }
                @if (userRole == "Admin")
                {
                    <li class="nav-item">
                        <a class="nav-link @(currentPath.StartsWith("/Admin/Usuarios") ? "active" : "")" href="/Admin/Usuarios"><i class="bi bi-people me-1"></i>Usuarios</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentPath.StartsWith("/Admin/Parametros") ? "active" : "")" href="/Admin/Parametros"><i class="bi bi-sliders me-1"></i>Parámetros</a>
                    </li>
                }
            </ul>
            <ul class="navbar-nav ms-auto align-items-lg-center gap-2">
                @if (!string.IsNullOrEmpty(userRole))
                {
                    <li class="nav-item">
                        <span class="badge bg-secondary px-2 py-1">@userRole</span>
                    </li>
                }
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center gap-2"
                       href="#" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle fs-5"></i>
                        <span>@userName</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end shadow">
                        <li><a class="dropdown-item @(currentPath == "/Home/Perfil" ? "active" : "")" href="/Home/Perfil"><i class="bi bi-person me-2 text-primary"></i>Mi Perfil</a></li>
                        <li><hr class="dropdown-divider" /></li>
                        <li><a class="dropdown-item text-danger" href="/Auth/LogoutGet"><i class="bi bi-box-arrow-right me-2"></i>Cerrar Sesión</a></li>
                    </ul>
                </li>
            </ul>
=======
    @inject IHttpContextAccessor Accesor
    @{
        var userName = Accesor.HttpContext?.Session.GetString("UserName") ?? "Usuario";
        var userRole = Accesor.HttpContext?.Session.GetString("UserRole") ?? "";
    }

    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
        <div class="container-fluid px-4">

            <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/Home">
                <i class="bi bi-tree-fill fs-4"></i>
                <span>Sistema Nativa</span>
            </a>

            <button class="navbar-toggler border-0" type="button"
                    data-bs-toggle="collapse" data-bs-target="#navMain">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navMain">

                <!-- ENLACES IZQUIERDA -->
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                    <li class="nav-item">
                        <a class="nav-link" href="/Home">
                            <i class="bi bi-speedometer2 me-1"></i>Dashboard
                        </a>
                    </li>

                    @if (userRole == "Dueno")
                    {
                        <li class="nav-item">
                            <a class="nav-link" href="/Owner/MyProperties">
                                <i class="bi bi-map me-1"></i>Mis Fincas
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/Owner/PaymentHistory">
                                <i class="bi bi-cash-coin me-1"></i>Historial de Pagos
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/Owner/BankAccount">
                                <i class="bi bi-credit-card me-1"></i>Cuenta Bancaria
                            </a>
                        </li>
                    }

                    @if (userRole == "Ingeniero")
                    {
                        <li class="nav-item">
                            <a class="nav-link" href="/Engineer/FifoQueue">
                                <i class="bi bi-list-task me-1"></i>Cola FIFO
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/Engineer/Evaluation">
                                <i class="bi bi-clipboard-check me-1"></i>Evaluación
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/Engineer/History">
                                <i class="bi bi-clock-history me-1"></i>Historial
                            </a>
                        </li>
                    }

                    @if (userRole == "Admin")
                    {
                        <li class="nav-item">
                            <a class="nav-link" href="/Admin/Users">
                                <i class="bi bi-people me-1"></i>Usuarios
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/Admin/Properties">
                                <i class="bi bi-map me-1"></i>Fincas
                            </a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                                <i class="bi bi-gear me-1"></i>Configuración
                            </a>
                            <ul class="dropdown-menu shadow border-0">
                                <li>
                                    <a class="dropdown-item" href="/Admin/PaymentSettings">
                                        <i class="bi bi-sliders me-2 text-primary"></i>Parámetros de Pago
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="/Admin/AuditLog">
                                        <i class="bi bi-journal-text me-2 text-primary"></i>Log de Auditoría
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="/Admin/Reports">
                                        <i class="bi bi-bar-chart me-2 text-primary"></i>Reportes
                                    </a>
                                </li>
                            </ul>
                        </li>
                    }

                </ul>

                <!-- ENLACES DERECHA -->
                <ul class="navbar-nav ms-auto align-items-lg-center gap-2">

                    @if (!string.IsNullOrEmpty(userRole))
                    {
                        <li class="nav-item">
                            <span class="badge bg-secondary px-2 py-1">@userRole</span>
                        </li>
                    }

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center gap-2"
                           href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle fs-5"></i>
                            <span>@userName</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0">
                            @if (userRole == "Dueno")
                            {
                                <li>
                                    <a class="dropdown-item" href="/Owner/Profile">
                                        <i class="bi bi-person me-2 text-primary"></i>Mi Perfil
                                    </a>
                                </li>
                            }
                            else if (userRole == "Ingeniero")
                            {
                                <li>
                                    <a class="dropdown-item" href="/Engineer/Profile">
                                        <i class="bi bi-person me-2 text-primary"></i>Mi Perfil
                                    </a>
                                </li>
                            }
                            else if (userRole == "Admin")
                            {
                                <li>
                                    <a class="dropdown-item" href="/Admin/Profile">
                                        <i class="bi bi-person me-2 text-primary"></i>Mi Perfil
                                    </a>
                                </li>
                            }
                            <li><hr class="dropdown-divider" /></li>
                            <li>
                                <a class="dropdown-item text-danger" href="/Login/Logout">
                                    <i class="bi bi-box-arrow-right me-2"></i>Cerrar Sesión
                                </a>
                            </li>
                        </ul>
                    </li>

                </ul>
            </div>
>>>>>>> 8938498ba942204ca8456128102b364380d3999e
        </div>
    </nav>

    <!-- CONTENIDO -->
    <main class="flex-grow-1 py-4">
        <div class="container-fluid px-4">
            @RenderBody()
        </div>
    </main>

    <!-- FOOTER -->
    <footer class="bg-light border-top py-3 mt-auto">
        <div class="container-fluid px-4">
            <div class="d-flex flex-column flex-md-row justify-content-between align-items-center gap-1">
                <span class="text-muted small">
                    <i class="bi bi-tree-fill text-success me-1"></i>
                    Sistema Nativa &copy; @DateTime.Now.Year
                </span>
                <span class="text-muted small">
                    Pago por Servicios Ambientales &mdash; Costa Rica
                </span>
            </div>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>